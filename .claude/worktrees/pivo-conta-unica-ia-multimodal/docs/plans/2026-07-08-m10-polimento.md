# M10 — Polimento: Plano de Implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fechar os requisitos funcionais restantes da seção 3 do PRD: busca e filtros na fila/acervo, relatórios e exportação (incl. relatório semanal por e-mail), calendário visual de posts agendados, e revisão geral de notificações (fechando os débitos técnicos de notificação registrados nos M6/M9).

**Architecture:** Todas as features são camadas de leitura/apresentação sobre o modelo de dados existente — **nenhuma migration nova é necessária**. Lógica pura isolada em módulos novos de `lib/` (mesmo padrão do `lib/acervo/scheduler.ts` do M8): filtros em `lib/posts/filterPosts.ts`, calendário em `lib/calendar/month.ts`, CSV/relatórios em `lib/reports/`. UI client-side só onde há estado de interação (barra de filtros, navegação de mês). O relatório semanal é um cron novo (`weekly-report`), mesmo padrão de auth `CRON_SECRET` fail-closed dos 7 crons existentes.

**Tech Stack:** Next.js 16 (App Router), React 19, TypeScript, Tailwind 4, shadcn/ui, Supabase (leitura via RLS no painel; service-role só no cron), Resend.

## Global Constraints

- **Regra de ouro**: nenhum caminho de publicação sem aprovação humana — este milestone não toca no pipeline de publicação.
- **Nunca falhar em silêncio**: toda falha de query/envio ganha `console.error` com prefixo `[<contexto>]`; falhas de coleta de métricas ganham indicador visível no dashboard (débito do M9 fechado aqui).
- Sem framework de testes no projeto (decisão mantida dos M1–M9): verificação por task = `npx tsc --noEmit` + `npm run lint` + `npm run build` limpos, revisor por task, revisão final de branch. Lógica pura fica isolada em `lib/` para permitir testes futuros.
- Identidade visual: dark, acento `#96DB12` (`text-primary`/`bg-primary`), mesmos padrões de card/borda (`rounded-lg border border-border bg-muted/20`) já usados nos boards.
- Fuso horário de exibição/agrupamento: `America/Sao_Paulo` fixo, sem lib de fuso (mesmo padrão do M8/M9, via `Intl.DateTimeFormat`).
- Textos de UI e commits em português; commits com `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`; push para `origin/main` após cada commit na main (branch de feature: push da branch).
- **Não commitar** `lib/demo/` nem os blocos `DEMO_MODE` presentes no working tree de `lib/posts/queries.ts` e `lib/auth/get-current-profile.ts` (são locais, marcados "NÃO COMMITAR"). A branch do M10 parte da `main` limpa.
- Novas rotas de página entram em `app/(dashboard)/` (herdam sidebar + `dynamic = "force-dynamic"` do layout já força runtime dinâmico onde necessário — replicar `export const dynamic = "force-dynamic"` na página, como as demais).

**Branch de trabalho:** `m10-polimento` (criada a partir de `origin/main`; merge na `main` + push só na Task 11, mediado pelo controlador — subagentes só commitam na branch, nunca mergeiam/pusham para a main).

---

### Task 1: Lógica pura de filtros de posts (`lib/posts/filterPosts.ts`)

**Files:**
- Create: `lib/posts/filterPosts.ts`

**Interfaces:**
- Produces: `interface PostFilters { query: string; artistId: string | null; socialAccountId: string | null; postType: PostType | null; contentSource: ContentSource | null; }`, `const EMPTY_POST_FILTERS: PostFilters`, `function matchesPostFilters(post: PostWithRelations, filters: PostFilters): boolean`, `function hasActiveFilters(filters: PostFilters): boolean` — consumidos pelas Tasks 2 e 3.

- [ ] **Step 1: Criar o módulo com a lógica pura**

```typescript
// lib/posts/filterPosts.ts
import type {
  ContentSource,
  PostType,
  PostWithRelations,
} from "@/lib/types/post";

export interface PostFilters {
  /** Busca textual livre (manchete, legenda, artista, música, fato). */
  query: string;
  artistId: string | null;
  socialAccountId: string | null;
  postType: PostType | null;
  contentSource: ContentSource | null;
}

export const EMPTY_POST_FILTERS: PostFilters = {
  query: "",
  artistId: null,
  socialAccountId: null,
  postType: null,
  contentSource: null,
};

export function hasActiveFilters(filters: PostFilters): boolean {
  return (
    filters.query.trim() !== "" ||
    filters.artistId !== null ||
    filters.socialAccountId !== null ||
    filters.postType !== null ||
    filters.contentSource !== null
  );
}

/**
 * Normaliza para busca insensível a caixa e acentos ("LANÇAMENTO" casa
 * com "lancamento") — conteúdo do painel é pt-BR, acentuação varia entre
 * manchete gerada por IA e digitação manual.
 */
function normalize(value: string): string {
  return value
    .normalize("NFD")
    .replace(/[̀-ͯ]/g, "")
    .toLowerCase();
}

export function matchesPostFilters(
  post: PostWithRelations,
  filters: PostFilters
): boolean {
  if (filters.artistId !== null && post.artist_id !== filters.artistId) {
    return false;
  }
  if (
    filters.socialAccountId !== null &&
    post.social_account_id !== filters.socialAccountId
  ) {
    return false;
  }
  if (filters.postType !== null && post.post_type !== filters.postType) {
    return false;
  }
  if (
    filters.contentSource !== null &&
    post.content_source !== filters.contentSource
  ) {
    return false;
  }

  const query = normalize(filters.query.trim());
  if (query === "") return true;

  const haystack = [
    post.headline,
    post.caption,
    post.track_name,
    post.source_fact,
    post.artist?.name ?? null,
    post.artist?.handle ?? null,
    post.social_account?.display_name ?? null,
    post.social_account?.handle ?? null,
  ]
    .filter((part): part is string => part !== null)
    .map(normalize)
    .join("\n");

  return haystack.includes(query);
}
```

- [ ] **Step 2: Verificar**

Run: `npx tsc --noEmit` e `npm run lint`
Expected: sem erros novos (só os warnings pré-existentes de `<img>`/`alt` documentados).

- [ ] **Step 3: Commit**

```bash
git add lib/posts/filterPosts.ts
git commit -m "feat(m10): lógica pura de busca e filtros de posts"
```

---

### Task 2: Barra de filtros + integração na fila `/conteudo`

**Files:**
- Create: `components/kanban/board-filters.tsx` (client component: barra de busca + selects)
- Create: `components/kanban/filterable-board.tsx` (client component: estado dos filtros + `KanbanBoard` filtrado)
- Modify: `app/(dashboard)/conteudo/page.tsx` (troca `KanbanBoard` por `FilterableBoard`)

**Interfaces:**
- Consumes: `PostFilters`, `EMPTY_POST_FILTERS`, `matchesPostFilters`, `hasActiveFilters` (Task 1); `KanbanBoard` existente (`components/kanban/board.tsx` — não muda).
- Produces: `<BoardFilters filters onChange artists socialAccounts showContentSource showPostType resultCount totalCount />` (reusado na Task 3); `<FilterableBoard posts currentUserId role artists socialAccounts />`.

- [ ] **Step 1: Criar `components/kanban/board-filters.tsx`**

Barra horizontal acima do board, mesmo vocabulário visual dos formulários existentes (`Input` do shadcn, selects nativos `<select>` estilizados como os já usados em `post-form-dialog.tsx`). Estrutura:

```tsx
"use client";

import { Input } from "@/components/ui/input";
import type { Artist } from "@/lib/types/artist";
import type { SocialAccount } from "@/lib/types/social-account";
import {
  CONTENT_SOURCES,
  POST_TYPES,
  POST_TYPE_LABELS,
  type ContentSource,
  type PostType,
} from "@/lib/types/post";
import {
  EMPTY_POST_FILTERS,
  hasActiveFilters,
  type PostFilters,
} from "@/lib/posts/filterPosts";

const CONTENT_SOURCE_LABELS: Record<ContentSource, string> = {
  drive: "Drive",
  acervo: "Acervo",
};

const SELECT_CLASSES =
  "h-9 rounded-md border border-input bg-transparent px-2 text-sm text-foreground";

export function BoardFilters({
  filters,
  onChange,
  artists,
  socialAccounts,
  showContentSource = true,
  showPostType = true,
  resultCount,
  totalCount,
}: {
  filters: PostFilters;
  onChange: (filters: PostFilters) => void;
  artists: Artist[];
  socialAccounts: SocialAccount[];
  showContentSource?: boolean;
  showPostType?: boolean;
  resultCount: number;
  totalCount: number;
}) {
  return (
    <div className="flex flex-wrap items-center gap-2">
      <Input
        value={filters.query}
        onChange={(e) => onChange({ ...filters, query: e.target.value })}
        placeholder="Buscar por manchete, legenda, artista, música..."
        className="h-9 w-full sm:max-w-xs"
        aria-label="Buscar posts"
      />
      <select
        className={SELECT_CLASSES}
        value={filters.artistId ?? ""}
        onChange={(e) =>
          onChange({ ...filters, artistId: e.target.value || null })
        }
        aria-label="Filtrar por artista"
      >
        <option value="">Todos os artistas</option>
        {artists.map((artist) => (
          <option key={artist.id} value={artist.id}>
            {artist.name}
          </option>
        ))}
      </select>
      <select
        className={SELECT_CLASSES}
        value={filters.socialAccountId ?? ""}
        onChange={(e) =>
          onChange({ ...filters, socialAccountId: e.target.value || null })
        }
        aria-label="Filtrar por conta social"
      >
        <option value="">Todas as contas</option>
        {socialAccounts.map((account) => (
          <option key={account.id} value={account.id}>
            {account.display_name}
          </option>
        ))}
      </select>
      {showPostType && (
        <select
          className={SELECT_CLASSES}
          value={filters.postType ?? ""}
          onChange={(e) =>
            onChange({
              ...filters,
              postType: (e.target.value || null) as PostType | null,
            })
          }
          aria-label="Filtrar por tipo de post"
        >
          <option value="">Todos os tipos</option>
          {POST_TYPES.map((type) => (
            <option key={type} value={type}>
              {POST_TYPE_LABELS[type]}
            </option>
          ))}
        </select>
      )}
      {showContentSource && (
        <select
          className={SELECT_CLASSES}
          value={filters.contentSource ?? ""}
          onChange={(e) =>
            onChange({
              ...filters,
              contentSource: (e.target.value || null) as ContentSource | null,
            })
          }
          aria-label="Filtrar por origem"
        >
          <option value="">Todas as origens</option>
          {CONTENT_SOURCES.map((source) => (
            <option key={source} value={source}>
              {CONTENT_SOURCE_LABELS[source]}
            </option>
          ))}
        </select>
      )}
      {hasActiveFilters(filters) && (
        <>
          <button
            type="button"
            onClick={() => onChange(EMPTY_POST_FILTERS)}
            className="text-xs text-primary underline-offset-2 hover:underline"
          >
            Limpar filtros
          </button>
          <span className="text-xs text-muted-foreground" aria-live="polite">
            {resultCount} de {totalCount} posts
          </span>
        </>
      )}
    </div>
  );
}
```

- [ ] **Step 2: Criar `components/kanban/filterable-board.tsx`**

```tsx
"use client";

import { useState } from "react";

import { KanbanBoard } from "@/components/kanban/board";
import { BoardFilters } from "@/components/kanban/board-filters";
import {
  EMPTY_POST_FILTERS,
  matchesPostFilters,
  type PostFilters,
} from "@/lib/posts/filterPosts";
import type { Artist } from "@/lib/types/artist";
import type { PostWithRelations } from "@/lib/types/post";
import type { Role } from "@/lib/types/profile";
import type { SocialAccount } from "@/lib/types/social-account";

export function FilterableBoard({
  posts,
  currentUserId,
  role,
  artists,
  socialAccounts,
}: {
  posts: PostWithRelations[];
  currentUserId: string;
  role: Role;
  artists: Artist[];
  socialAccounts: SocialAccount[];
}) {
  const [filters, setFilters] = useState<PostFilters>(EMPTY_POST_FILTERS);
  const filtered = posts.filter((post) => matchesPostFilters(post, filters));

  return (
    <div className="flex flex-col gap-4">
      <BoardFilters
        filters={filters}
        onChange={setFilters}
        artists={artists}
        socialAccounts={socialAccounts}
        resultCount={filtered.length}
        totalCount={posts.length}
      />
      <KanbanBoard
        posts={filtered}
        currentUserId={currentUserId}
        role={role}
        artists={artists}
        socialAccounts={socialAccounts}
      />
    </div>
  );
}
```

- [ ] **Step 3: Trocar o board em `app/(dashboard)/conteudo/page.tsx`**

Substituir o import e o uso de `KanbanBoard` por `FilterableBoard` (mesmas props — a assinatura foi copiada de propósito). **Também aplicar em `app/(dashboard)/aprovacao/page.tsx`** (ler o arquivo antes; ele monta o mesmo `KanbanBoard` — a fila de aprovação ganha os mesmos filtros de graça).

- [ ] **Step 4: Verificar**

Run: `npx tsc --noEmit && npm run lint && npm run build`
Expected: limpo (warnings pré-existentes apenas).

- [ ] **Step 5: Commit**

```bash
git add components/kanban/board-filters.tsx components/kanban/filterable-board.tsx "app/(dashboard)/conteudo/page.tsx" "app/(dashboard)/aprovacao/page.tsx"
git commit -m "feat(m10): busca e filtros na fila de posts e fila de aprovação"
```

---

### Task 3: Filtros no `/acervo`

**Files:**
- Create: `components/acervo/filterable-acervo-board.tsx` (client)
- Modify: `app/(dashboard)/acervo/page.tsx`

**Interfaces:**
- Consumes: `BoardFilters` (Task 2, com `showPostType={false}` e `showContentSource={false}` — acervo é sempre `content_source='acervo'` e o tipo não é relevante lá), `matchesPostFilters` (Task 1), `AcervoBoard` existente (não muda).

- [ ] **Step 1: Criar `components/acervo/filterable-acervo-board.tsx`**

Mesmo padrão da Task 2 (estado `useState<PostFilters>`, `posts.filter(matchesPostFilters)`, `BoardFilters` acima do `AcervoBoard`), com `showPostType={false}` e `showContentSource={false}`. Props: `{ posts, artists, socialAccounts }` (ler `app/(dashboard)/acervo/page.tsx` para ver o que a página já carrega — se `artists`/`socialAccounts` ainda não são buscados lá, adicionar `listArtists()`/`listSocialAccounts()` ao `Promise.all` da página).

- [ ] **Step 2: Integrar na página, verificar (`npx tsc --noEmit && npm run lint && npm run build`) e commitar**

```bash
git add components/acervo/filterable-acervo-board.tsx "app/(dashboard)/acervo/page.tsx"
git commit -m "feat(m10): busca e filtros no acervo"
```

---

### Task 4: Lógica pura do calendário (`lib/calendar/month.ts`)

**Files:**
- Create: `lib/calendar/month.ts`

**Interfaces:**
- Produces (consumidos pela Task 5):
  - `interface CalendarDay { iso: string; day: number; inMonth: boolean; }` (`iso` = `"YYYY-MM-DD"`)
  - `function buildMonthGrid(year: number, month: number): CalendarDay[][]` — semanas de 7 dias (domingo a sábado), incluindo dias dos meses vizinhos com `inMonth: false`; `month` é 1–12.
  - `function dayKeyInSaoPaulo(isoTimestamp: string): string` — `"YYYY-MM-DD"` no fuso `America/Sao_Paulo`.
  - `function groupPostsByDay(posts: PostWithRelations[]): Map<string, PostWithRelations[]>` — chave = dia do `scheduled_at ?? published_at` em São Paulo; posts sem ambos são ignorados.
  - `function monthLabel(year: number, month: number): string` — ex.: `"julho de 2026"`.

- [ ] **Step 1: Implementar**

```typescript
// lib/calendar/month.ts
import type { PostWithRelations } from "@/lib/types/post";

export interface CalendarDay {
  /** "YYYY-MM-DD" — chave estável do dia, sem hora/fuso. */
  iso: string;
  day: number;
  inMonth: boolean;
}

function pad(n: number): string {
  return String(n).padStart(2, "0");
}

/**
 * Grade mensal domingo→sábado. Datas manipuladas como UTC puro (Date.UTC)
 * de propósito: aqui só interessa aritmética de dias-do-calendário, sem
 * hora — o fuso só entra ao converter timestamps de posts (dayKeyInSaoPaulo).
 */
export function buildMonthGrid(year: number, month: number): CalendarDay[][] {
  const first = new Date(Date.UTC(year, month - 1, 1));
  const start = new Date(first);
  start.setUTCDate(1 - first.getUTCDay()); // volta ao domingo da 1ª semana

  const weeks: CalendarDay[][] = [];
  const cursor = new Date(start);
  do {
    const week: CalendarDay[] = [];
    for (let i = 0; i < 7; i++) {
      week.push({
        iso: `${cursor.getUTCFullYear()}-${pad(cursor.getUTCMonth() + 1)}-${pad(cursor.getUTCDate())}`,
        day: cursor.getUTCDate(),
        inMonth: cursor.getUTCMonth() === month - 1,
      });
      cursor.setUTCDate(cursor.getUTCDate() + 1);
    }
    weeks.push(week);
  } while (cursor.getUTCMonth() === month - 1);
  return weeks;
}

/** Dia-calendário de um timestamp no fuso America/Sao_Paulo ("YYYY-MM-DD"). */
export function dayKeyInSaoPaulo(isoTimestamp: string): string {
  // en-CA formata como YYYY-MM-DD, dispensando remontagem manual de parts.
  return new Intl.DateTimeFormat("en-CA", {
    timeZone: "America/Sao_Paulo",
    year: "numeric",
    month: "2-digit",
    day: "2-digit",
  }).format(new Date(isoTimestamp));
}

export function groupPostsByDay(
  posts: PostWithRelations[]
): Map<string, PostWithRelations[]> {
  const byDay = new Map<string, PostWithRelations[]>();
  for (const post of posts) {
    const timestamp = post.scheduled_at ?? post.published_at;
    if (!timestamp) continue;
    const key = dayKeyInSaoPaulo(timestamp);
    const list = byDay.get(key) ?? [];
    list.push(post);
    byDay.set(key, list);
  }
  // Ordena os posts de cada dia por horário.
  for (const list of byDay.values()) {
    list.sort((a, b) =>
      (a.scheduled_at ?? a.published_at ?? "").localeCompare(
        b.scheduled_at ?? b.published_at ?? ""
      )
    );
  }
  return byDay;
}

export function monthLabel(year: number, month: number): string {
  return new Intl.DateTimeFormat("pt-BR", {
    month: "long",
    year: "numeric",
    timeZone: "UTC",
  }).format(new Date(Date.UTC(year, month - 1, 1)));
}
```

- [ ] **Step 2: Verificar (`npx tsc --noEmit && npm run lint`) e commitar**

```bash
git add lib/calendar/month.ts
git commit -m "feat(m10): lógica pura da grade mensal do calendário"
```

---

### Task 5: Rota `/calendario` + componente de calendário + item na sidebar

**Files:**
- Create: `app/(dashboard)/calendario/page.tsx`
- Create: `components/calendar/month-calendar.tsx`
- Modify: `components/dashboard/nav-items.ts` (novo item "Calendário", grupo Operação, todos os papéis, ícone `CalendarDays` de lucide-react)

**Interfaces:**
- Consumes: Task 4 inteira; `listPosts()` de `lib/posts/queries.ts`; `PageHeader`; `getCurrentProfile`.
- Navegação de mês via **querystring** (`/calendario?mes=2026-07`), não estado client — a página continua server component, links `<Link>` para mês anterior/seguinte. Sem mudança em `roleAllowsRoute()` (`lib/supabase/proxy.ts` já libera rotas não-admin/aprovação para todos os papéis).

- [ ] **Step 1: Criar `app/(dashboard)/calendario/page.tsx`**

```tsx
import { PageHeader } from "@/components/dashboard/page-header";
import { MonthCalendar } from "@/components/calendar/month-calendar";
import { listPosts } from "@/lib/posts/queries";

export const dynamic = "force-dynamic";

function parseMonthParam(mes: string | undefined): { year: number; month: number } {
  const match = mes?.match(/^(\d{4})-(\d{2})$/);
  if (match) {
    const year = Number(match[1]);
    const month = Number(match[2]);
    if (month >= 1 && month <= 12) return { year, month };
  }
  // Default: mês corrente em São Paulo.
  const now = new Intl.DateTimeFormat("en-CA", {
    timeZone: "America/Sao_Paulo",
    year: "numeric",
    month: "2-digit",
  }).format(new Date()); // "YYYY-MM"
  const [year, month] = now.split("-").map(Number);
  return { year, month };
}

export default async function CalendarioPage({
  searchParams,
}: {
  searchParams: Promise<{ mes?: string }>;
}) {
  const { mes } = await searchParams;
  const { year, month } = parseMonthParam(mes);

  const posts = await listPosts();
  // Só posts com horário definido interessam no calendário: agendados
  // (aprovado + scheduled_at) e publicados.
  const scheduled = posts.filter(
    (post) =>
      (post.status === "aprovado" && post.scheduled_at !== null) ||
      post.status === "publicado"
  );

  return (
    <div className="flex flex-1 flex-col gap-6 px-6 py-10 md:px-8">
      <PageHeader
        title="Calendário"
        description="Posts agendados e publicados por dia (horário de São Paulo)."
      />
      <MonthCalendar year={year} month={month} posts={scheduled} />
    </div>
  );
}
```

> Nota Next 16: conferir em `node_modules/next/dist/docs/` a forma vigente de `searchParams` (Promise, como acima) antes de implementar — `AGENTS.md` manda ler a doc da versão.

- [ ] **Step 2: Criar `components/calendar/month-calendar.tsx`** (server component, sem estado)

```tsx
import Link from "next/link";
import { ChevronLeft, ChevronRight } from "lucide-react";

import {
  buildMonthGrid,
  groupPostsByDay,
  monthLabel,
} from "@/lib/calendar/month";
import type { PostWithRelations } from "@/lib/types/post";

const WEEKDAYS = ["Dom", "Seg", "Ter", "Qua", "Qui", "Sex", "Sáb"];

function monthHref(year: number, month: number, delta: number): string {
  const d = new Date(Date.UTC(year, month - 1 + delta, 1));
  const mm = String(d.getUTCMonth() + 1).padStart(2, "0");
  return `/calendario?mes=${d.getUTCFullYear()}-${mm}`;
}

function timeInSaoPaulo(isoTimestamp: string): string {
  return new Intl.DateTimeFormat("pt-BR", {
    timeZone: "America/Sao_Paulo",
    hour: "2-digit",
    minute: "2-digit",
  }).format(new Date(isoTimestamp));
}

export function MonthCalendar({
  year,
  month,
  posts,
}: {
  year: number;
  month: number;
  posts: PostWithRelations[];
}) {
  const weeks = buildMonthGrid(year, month);
  const byDay = groupPostsByDay(posts);

  return (
    <div className="flex flex-col gap-4">
      <div className="flex items-center justify-between">
        <h2 className="text-sm font-semibold capitalize text-foreground">
          {monthLabel(year, month)}
        </h2>
        <div className="flex items-center gap-2">
          <Link
            href={monthHref(year, month, -1)}
            className="rounded-md border border-border p-1.5 text-muted-foreground hover:text-foreground"
            aria-label="Mês anterior"
          >
            <ChevronLeft className="size-4" />
          </Link>
          <Link
            href={monthHref(year, month, +1)}
            className="rounded-md border border-border p-1.5 text-muted-foreground hover:text-foreground"
            aria-label="Próximo mês"
          >
            <ChevronRight className="size-4" />
          </Link>
        </div>
      </div>

      <div className="overflow-x-auto">
        <div className="min-w-[640px]">
          <div className="grid grid-cols-7 gap-px">
            {WEEKDAYS.map((label) => (
              <div
                key={label}
                className="px-2 py-1 text-xs font-medium text-muted-foreground"
              >
                {label}
              </div>
            ))}
          </div>
          {weeks.map((week, i) => (
            <div key={i} className="grid grid-cols-7 gap-px">
              {week.map((day) => {
                const dayPosts = byDay.get(day.iso) ?? [];
                return (
                  <div
                    key={day.iso}
                    className={`min-h-24 rounded-sm border border-border/50 p-1.5 ${
                      day.inMonth ? "bg-muted/20" : "bg-transparent opacity-40"
                    }`}
                  >
                    <span className="text-xs text-muted-foreground">
                      {day.day}
                    </span>
                    <div className="mt-1 flex flex-col gap-1">
                      {dayPosts.map((post) => (
                        <div
                          key={post.id}
                          className={`truncate rounded px-1 py-0.5 text-[11px] leading-tight ${
                            post.status === "publicado"
                              ? "bg-primary/15 text-primary"
                              : "bg-muted text-foreground"
                          }`}
                          title={`${timeInSaoPaulo(
                            post.scheduled_at ?? post.published_at ?? ""
                          )} — ${post.headline ?? post.caption ?? "Post"}`}
                        >
                          {timeInSaoPaulo(
                            post.scheduled_at ?? post.published_at ?? ""
                          )}{" "}
                          {post.headline ?? post.caption ?? "Post"}
                        </div>
                      ))}
                    </div>
                  </div>
                );
              })}
            </div>
          ))}
        </div>
      </div>

      <div className="flex items-center gap-4 text-xs text-muted-foreground">
        <span className="flex items-center gap-1.5">
          <span className="size-2 rounded-full bg-muted-foreground/40" /> Agendado
        </span>
        <span className="flex items-center gap-1.5">
          <span className="size-2 rounded-full bg-primary" /> Publicado
        </span>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: Adicionar o item na sidebar** (`components/dashboard/nav-items.ts`, grupo "Operação", depois de "Acervo")

```typescript
{
  title: "Calendário",
  url: "/calendario",
  icon: CalendarDays,
  roles: ["admin", "aprovador", "equipe_conteudo"],
},
```

(e adicionar `CalendarDays` ao import de `lucide-react`.)

- [ ] **Step 4: Verificar (`npx tsc --noEmit && npm run lint && npm run build`) e commitar**

```bash
git add "app/(dashboard)/calendario/page.tsx" components/calendar/month-calendar.tsx components/dashboard/nav-items.ts
git commit -m "feat(m10): calendário visual de posts agendados e publicados"
```

---

### Task 6: Lógica pura de relatórios (`lib/reports/csv.ts` + `lib/reports/postsReport.ts`)

**Files:**
- Create: `lib/reports/csv.ts`
- Create: `lib/reports/postsReport.ts`

**Interfaces:**
- Produces:
  - `function toCsv(headers: string[], rows: (string | number | null)[][]): string` — CSV RFC-4180 (aspas duplas escapadas, campos com `,`/`"`/quebra de linha entre aspas), linhas separadas por `\r\n`, **com BOM UTF-8** no início (Excel pt-BR).
  - `interface PostsReportRow` e `function buildPostsReportRows(posts: PostReportInput[]): { headers: string[]; rows: (string | number | null)[][] }` — consumidos pela Task 7.

- [ ] **Step 1: Implementar `lib/reports/csv.ts`**

```typescript
// lib/reports/csv.ts

function escapeField(value: string | number | null): string {
  if (value === null) return "";
  const text = String(value);
  if (/[",\r\n]/.test(text)) {
    return `"${text.replace(/"/g, '""')}"`;
  }
  return text;
}

/**
 * CSV RFC-4180 com BOM UTF-8 (Excel pt-BR abre com acentuação correta
 * sem importação manual).
 */
export function toCsv(
  headers: string[],
  rows: (string | number | null)[][]
): string {
  const lines = [headers, ...rows].map((row) =>
    row.map(escapeField).join(",")
  );
  return "﻿" + lines.join("\r\n") + "\r\n";
}
```

- [ ] **Step 2: Implementar `lib/reports/postsReport.ts`**

```typescript
// lib/reports/postsReport.ts
import {
  POST_STATUS_LABELS,
  POST_TYPE_LABELS,
  type PostStatus,
  type PostType,
  type ContentSource,
} from "@/lib/types/post";

/** Shape mínimo consumido do join posts + post_metrics (ver Task 7). */
export interface PostReportInput {
  id: string;
  status: PostStatus;
  post_type: PostType;
  content_source: ContentSource;
  headline: string | null;
  caption: string | null;
  scheduled_at: string | null;
  published_at: string | null;
  post_url: string | null;
  created_at: string;
  artist: { name: string } | null;
  social_account: { display_name: string } | null;
  metrics: {
    likes: number | null;
    comments: number | null;
    reach: number | null;
  } | null;
}

const HEADERS = [
  "id",
  "status",
  "tipo",
  "origem",
  "artista",
  "conta",
  "manchete",
  "legenda",
  "criado_em",
  "agendado_para",
  "publicado_em",
  "link",
  "curtidas",
  "comentarios",
  "alcance",
];

export function buildPostsReportRows(posts: PostReportInput[]): {
  headers: string[];
  rows: (string | number | null)[][];
} {
  return {
    headers: HEADERS,
    rows: posts.map((post) => [
      post.id,
      POST_STATUS_LABELS[post.status],
      POST_TYPE_LABELS[post.post_type],
      post.content_source,
      post.artist?.name ?? null,
      post.social_account?.display_name ?? null,
      post.headline,
      post.caption,
      post.created_at,
      post.scheduled_at,
      post.published_at,
      post.post_url,
      post.metrics?.likes ?? null,
      post.metrics?.comments ?? null,
      post.metrics?.reach ?? null,
    ]),
  };
}
```

- [ ] **Step 3: Verificar (`npx tsc --noEmit && npm run lint`) e commitar**

```bash
git add lib/reports/csv.ts lib/reports/postsReport.ts
git commit -m "feat(m10): lógica pura de geração de CSV do relatório de posts"
```

---

### Task 7: Exportação CSV — rota `GET /api/reports/posts` + botão no dashboard

**Files:**
- Create: `app/api/reports/posts/route.ts`
- Modify: `app/(dashboard)/dashboard/page.tsx` (botão/link "Exportar CSV" na seção de analytics)

**Interfaces:**
- Consumes: Task 6; `createClient` de `lib/supabase/server.ts` (sessão do usuário → **RLS se aplica**, sem service-role aqui de propósito: exportação roda com as permissões de quem exporta).
- Auth: o `proxy.ts` já retorna 401 para `/api/*` sem sessão — checagem extra de `auth.getUser()` na rota como defesa em profundidade (mesmo padrão do restante do app). Query param `dias` (default 30, clamp 1–365).

- [ ] **Step 1: Implementar a rota**

```typescript
// app/api/reports/posts/route.ts
import { NextResponse } from "next/server";

import { toCsv } from "@/lib/reports/csv";
import {
  buildPostsReportRows,
  type PostReportInput,
} from "@/lib/reports/postsReport";
import { createClient } from "@/lib/supabase/server";

export const dynamic = "force-dynamic";

export async function GET(request: Request) {
  const supabase = await createClient();
  const {
    data: { user },
  } = await supabase.auth.getUser();
  if (!user) {
    return NextResponse.json({ error: "unauthorized" }, { status: 401 });
  }

  const url = new URL(request.url);
  const rawDays = Number(url.searchParams.get("dias") ?? "30");
  const days = Number.isFinite(rawDays)
    ? Math.min(365, Math.max(1, Math.trunc(rawDays)))
    : 30;
  const cutoff = new Date(
    Date.now() - days * 24 * 60 * 60 * 1000
  ).toISOString();

  const { data, error } = await supabase
    .from("posts")
    .select(
      "id, status, post_type, content_source, headline, caption, scheduled_at, published_at, post_url, created_at, artist:artists(name), social_account:social_accounts(display_name), metrics:post_metrics(likes, comments, reach)"
    )
    .gte("created_at", cutoff)
    .order("created_at", { ascending: false });

  if (error) {
    console.error("[reports] falha ao montar relatório de posts:", error.message);
    return NextResponse.json({ error: "report_failed" }, { status: 500 });
  }

  // post_metrics tem no máximo 1 linha por post (PK/unique post_id) —
  // o Supabase devolve como array; achata para objeto único.
  const inputs: PostReportInput[] = ((data ?? []) as unknown as (Omit<
    PostReportInput,
    "metrics"
  > & { metrics: PostReportInput["metrics"][] })[]).map((row) => ({
    ...row,
    metrics: Array.isArray(row.metrics) ? row.metrics[0] ?? null : row.metrics,
  }));

  const { headers, rows } = buildPostsReportRows(inputs);
  const today = new Date().toISOString().slice(0, 10);

  return new NextResponse(toCsv(headers, rows), {
    headers: {
      "Content-Type": "text/csv; charset=utf-8",
      "Content-Disposition": `attachment; filename="posts-puzzle-${today}.csv"`,
    },
  });
}
```

> Antes de implementar, confirmar em `supabase/migrations/0010_analytics.sql` o nome exato da relação `post_metrics` e a unicidade de `post_id` (o `onConflict: "post_id"` do M9 indica unique). Se o alias `metrics:post_metrics(...)` não resolver pelo nome da FK, usar a sintaxe `post_metrics!post_id(...)`.

- [ ] **Step 2: Botão no dashboard**

Em `app/(dashboard)/dashboard/page.tsx`, junto ao título da seção de analytics, um link estilizado como botão outline (mesmo padrão visual dos botões secundários existentes):

```tsx
<a
  href="/api/reports/posts?dias=30"
  download
  className="inline-flex h-8 items-center rounded-md border border-border px-3 text-xs font-medium text-foreground hover:bg-muted/40"
>
  Exportar CSV (30 dias)
</a>
```

- [ ] **Step 3: Verificar (`npx tsc --noEmit && npm run lint && npm run build`) e commitar**

```bash
git add app/api/reports/posts/route.ts "app/(dashboard)/dashboard/page.tsx"
git commit -m "feat(m10): exportação CSV de posts e métricas pelo dashboard"
```

---

### Task 8: Agregação do relatório semanal (`lib/reports/weeklySummary.ts`)

**Files:**
- Create: `lib/reports/weeklySummary.ts`

**Interfaces:**
- Produces (consumidos pela Task 9):
  - `interface WeeklySummary { weekStartIso: string; weekEndIso: string; publishedCount: number; approvedPendingCount: number; failedCount: number; disconnectedAccounts: string[]; topPosts: { headline: string | null; caption: string | null; artistName: string | null; likes: number; comments: number; postUrl: string | null }[]; byAccount: { account: string; published: number; likes: number; comments: number; reach: number }[]; }`
  - `async function buildWeeklySummary(): Promise<WeeklySummary | { error: string }>` — usa `createServiceClient()` (é chamada por cron, sem sessão de usuário; mesmo padrão dos crons do M6–M9).

- [ ] **Step 1: Implementar**

```typescript
// lib/reports/weeklySummary.ts
import { createServiceClient } from "@/lib/supabase/service";

const WEEK_MS = 7 * 24 * 60 * 60 * 1000;

export interface WeeklySummary {
  weekStartIso: string;
  weekEndIso: string;
  publishedCount: number;
  approvedPendingCount: number;
  failedCount: number;
  disconnectedAccounts: string[];
  topPosts: {
    headline: string | null;
    caption: string | null;
    artistName: string | null;
    likes: number;
    comments: number;
    postUrl: string | null;
  }[];
  byAccount: {
    account: string;
    published: number;
    likes: number;
    comments: number;
    reach: number;
  }[];
}

export async function buildWeeklySummary(): Promise<
  WeeklySummary | { error: string }
> {
  const supabase = createServiceClient();
  const weekEnd = new Date();
  const weekStart = new Date(weekEnd.getTime() - WEEK_MS);

  const { data: published, error: publishedError } = await supabase
    .from("posts")
    .select(
      "headline, caption, post_url, published_at, artist:artists(name), social_account:social_accounts(display_name), metrics:post_metrics(likes, comments, reach)"
    )
    .eq("status", "publicado")
    .gte("published_at", weekStart.toISOString());

  if (publishedError) {
    return { error: `Falha ao buscar posts publicados: ${publishedError.message}` };
  }

  const [{ count: approvedPendingCount }, failedRes, disconnectedRes] =
    await Promise.all([
      supabase
        .from("posts")
        .select("id", { count: "exact", head: true })
        .eq("status", "aprovado"),
      supabase
        .from("posts")
        .select("id", { count: "exact", head: true })
        .not("publish_error", "is", null),
      supabase
        .from("social_accounts")
        .select("display_name")
        .eq("connection_status", "desconectada"),
    ]);

  type Row = {
    headline: string | null;
    caption: string | null;
    post_url: string | null;
    published_at: string | null;
    artist: { name: string } | null;
    social_account: { display_name: string } | null;
    metrics: { likes: number | null; comments: number | null; reach: number | null }[];
  };
  const rows = (published ?? []) as unknown as Row[];

  const byAccountMap = new Map<string, WeeklySummary["byAccount"][number]>();
  const scored = rows.map((row) => {
    const m = row.metrics?.[0] ?? null;
    const likes = m?.likes ?? 0;
    const comments = m?.comments ?? 0;
    const reach = m?.reach ?? 0;
    const account = row.social_account?.display_name ?? "Sem conta";
    const acc = byAccountMap.get(account) ?? {
      account,
      published: 0,
      likes: 0,
      comments: 0,
      reach: 0,
    };
    acc.published += 1;
    acc.likes += likes;
    acc.comments += comments;
    acc.reach += reach;
    byAccountMap.set(account, acc);
    return { row, likes, comments };
  });

  const topPosts = scored
    .sort((a, b) => b.likes + b.comments - (a.likes + a.comments))
    .slice(0, 5)
    .map(({ row, likes, comments }) => ({
      headline: row.headline,
      caption: row.caption,
      artistName: row.artist?.name ?? null,
      likes,
      comments,
      postUrl: row.post_url,
    }));

  return {
    weekStartIso: weekStart.toISOString(),
    weekEndIso: weekEnd.toISOString(),
    publishedCount: rows.length,
    approvedPendingCount: approvedPendingCount ?? 0,
    failedCount: failedRes.count ?? 0,
    disconnectedAccounts: (disconnectedRes.data ?? []).map(
      (a) => a.display_name
    ),
    topPosts,
    byAccount: Array.from(byAccountMap.values()).sort(
      (a, b) => b.published - a.published
    ),
  };
}
```

(Erros das queries secundárias de contagem: se `failedRes.error`/`disconnectedRes.error` vierem preenchidos, logar `console.error("[weekly-report] ...")` e seguir com 0/[] — o relatório parcial ainda é útil; a falha fica no log. Incluir essas checagens na implementação.)

- [ ] **Step 2: Verificar (`npx tsc --noEmit && npm run lint`) e commitar**

```bash
git add lib/reports/weeklySummary.ts
git commit -m "feat(m10): agregação do relatório semanal"
```

---

### Task 9: Cron `weekly-report` + template de e-mail

**Files:**
- Create: `app/api/cron/weekly-report/route.ts`
- Modify: `lib/email/templates.ts` (adicionar `weeklyReportSubject()` e `weeklyReportBody(summary)`)
- Modify: `vercel.json` (novo cron `0 12 * * 1` — segunda-feira 12:00 UTC = 09:00 em São Paulo)

**Interfaces:**
- Consumes: `buildWeeklySummary` (Task 8), `getApproverAndAdminEmails`, `getResendClient`/`EMAIL_FROM` de `lib/email/client.ts`, helpers `escapeHtml`/`sanitizeSubjectText` já existentes em `templates.ts`.
- Auth do cron: **copiar exatamente** o padrão fail-closed dos crons existentes (ler `app/api/cron/collect-metrics/route.ts` e replicar a checagem de `CRON_SECRET` — falha fechado se a env não estiver definida, como corrigido no M5).

- [ ] **Step 1: Templates em `lib/email/templates.ts`**

```typescript
export function weeklyReportSubject(weekEndIso: string) {
  const date = new Intl.DateTimeFormat("pt-BR", {
    timeZone: "America/Sao_Paulo",
    day: "2-digit",
    month: "2-digit",
  }).format(new Date(weekEndIso));
  return `📊 Relatório semanal Puzzle Records — semana até ${date}`;
}

export function weeklyReportBody(summary: WeeklySummary) {
  const url = `${process.env.NEXT_PUBLIC_SITE_URL ?? ""}/dashboard`;
  const top = summary.topPosts
    .map((post) => {
      const label = escapeHtml(post.headline ?? post.caption ?? "Post");
      const artist = post.artistName ? ` (${escapeHtml(post.artistName)})` : "";
      const link = post.postUrl
        ? ` — <a href="${post.postUrl}">ver post</a>`
        : "";
      return `<li>${label}${artist}: ${post.likes} curtidas, ${post.comments} comentários${link}</li>`;
    })
    .join("");
  const accounts = summary.byAccount
    .map(
      (a) =>
        `<li>${escapeHtml(a.account)}: ${a.published} posts, ${a.likes} curtidas, ${a.comments} comentários, alcance ${a.reach}</li>`
    )
    .join("");
  const disconnected =
    summary.disconnectedAccounts.length > 0
      ? `<p>⚠️ Contas possivelmente desconectadas: <strong>${summary.disconnectedAccounts
          .map(escapeHtml)
          .join(", ")}</strong></p>`
      : "";
  return (
    `<p>Resumo dos últimos 7 dias:</p>` +
    `<ul><li><strong>${summary.publishedCount}</strong> posts publicados</li>` +
    `<li><strong>${summary.approvedPendingCount}</strong> aprovados aguardando publicação</li>` +
    `<li><strong>${summary.failedCount}</strong> posts com erro de publicação</li></ul>` +
    (top ? `<p><strong>Top posts:</strong></p><ul>${top}</ul>` : "") +
    (accounts ? `<p><strong>Por conta:</strong></p><ul>${accounts}</ul>` : "") +
    disconnected +
    `<p><a href="${url}">Abrir o dashboard</a></p>`
  );
}
```

(Importar `type WeeklySummary` de `@/lib/reports/weeklySummary` no topo de `templates.ts`.)

- [ ] **Step 2: Rota do cron `app/api/cron/weekly-report/route.ts`**

Estrutura (replicando o esqueleto de auth de `collect-metrics`):

```typescript
import { NextResponse } from "next/server";

import { getResendClient, EMAIL_FROM } from "@/lib/email/client";
import { getApproverAndAdminEmails } from "@/lib/email/recipients";
import { weeklyReportSubject, weeklyReportBody } from "@/lib/email/templates";
import { buildWeeklySummary } from "@/lib/reports/weeklySummary";

export const dynamic = "force-dynamic";

function isAuthorized(request: Request): boolean {
  const secret = process.env.CRON_SECRET;
  if (!secret) return false; // fail-closed, mesmo padrão dos demais crons
  return request.headers.get("authorization") === `Bearer ${secret}`;
}

export async function GET(request: Request) {
  if (!isAuthorized(request)) {
    return NextResponse.json({ error: "unauthorized" }, { status: 401 });
  }

  const summary = await buildWeeklySummary();
  if ("error" in summary) {
    console.error("[weekly-report]", summary.error);
    return NextResponse.json({ error: summary.error }, { status: 500 });
  }

  const resend = getResendClient();
  if (!resend) {
    console.error("[weekly-report] RESEND_API_KEY não configurada.");
    return NextResponse.json({ error: "resend_not_configured" }, { status: 500 });
  }

  const recipients = await getApproverAndAdminEmails();
  if ("error" in recipients) {
    console.error("[weekly-report]", recipients.error);
    return NextResponse.json({ error: recipients.error }, { status: 500 });
  }

  const { error: sendError } = await resend.emails.send({
    from: EMAIL_FROM,
    to: EMAIL_FROM,
    bcc: recipients.emails,
    subject: weeklyReportSubject(summary.weekEndIso),
    html: weeklyReportBody(summary),
  });

  if (sendError) {
    console.error("[weekly-report] falha no envio:", sendError.message);
    return NextResponse.json({ error: sendError.message }, { status: 500 });
  }

  return NextResponse.json({ ok: true, published: summary.publishedCount });
}
```

> Antes de implementar, ler `app/api/cron/collect-metrics/route.ts` e alinhar o esqueleto (nome do header, formato de resposta) ao padrão real. O envio usa `to: EMAIL_FROM` + `bcc` destinatários — decisão da revisão de notificações (Task 10), já aplicada aqui.

- [ ] **Step 3: Registrar no `vercel.json`**

```json
{
  "path": "/api/cron/weekly-report",
  "schedule": "0 12 * * 1"
}
```

- [ ] **Step 4: Verificar (`npx tsc --noEmit && npm run lint && npm run build`) e commitar**

```bash
git add app/api/cron/weekly-report/route.ts lib/email/templates.ts vercel.json
git commit -m "feat(m10): relatório semanal por e-mail via cron"
```

---

### Task 10: Revisão geral de notificações (fecha débitos do M6/M9)

**Files:**
- Modify: `lib/email/notifyApprovers.ts` (bcc)
- Modify: `lib/email/notifyAccountDisconnected.ts` (bcc)
- Modify: `lib/email/templates.ts` (helper `absoluteUrl()` com aviso de env ausente)
- Modify: `lib/analytics/queries.ts` (nova função `countMetricsErrors()`)
- Modify: `components/dashboard/analytics-summary.tsx` + `app/(dashboard)/dashboard/page.tsx` (indicador de falhas de coleta)

Quatro itens, todos débitos registrados no PLAN.md:

- [ ] **Step 1: `bcc` nas notificações existentes** (débito M6 "e-mails não usam bcc entre destinatários")

Em `notifyApprovers.ts` e `notifyAccountDisconnected.ts`, trocar:

```typescript
// antes
to: recipients.emails,
// depois — destinatários não veem os e-mails uns dos outros
to: EMAIL_FROM,
bcc: recipients.emails,
```

- [ ] **Step 2: Centralizar link absoluto com aviso** (débito M6 "link relativo se NEXT_PUBLIC_SITE_URL não configurado")

Em `lib/email/templates.ts`, adicionar e usar em **todos** os corpos de e-mail (novo post, SLA, desconexão, relatório semanal):

```typescript
/**
 * Base absoluta dos links de e-mail. Sem NEXT_PUBLIC_SITE_URL o link sai
 * relativo (quebrado no cliente de e-mail) — loga aviso para o deploy
 * corrigir a env, sem bloquear o envio (aviso ruim > nenhum aviso).
 */
function absoluteUrl(path: string): string {
  const base = process.env.NEXT_PUBLIC_SITE_URL;
  if (!base) {
    console.warn(
      "[email] NEXT_PUBLIC_SITE_URL não configurada — links de e-mail sairão relativos."
    );
    return path;
  }
  return `${base}${path}`;
}
```

Substituir as 4 ocorrências de `` `${process.env.NEXT_PUBLIC_SITE_URL ?? ""}/...` `` por `absoluteUrl("/aprovacao")` etc.

- [ ] **Step 3: Indicador de falha de coleta de métricas no dashboard** (débito M9, explicitamente deixado para o M10)

Em `lib/analytics/queries.ts`:

```typescript
/**
 * Conta posts cuja última coleta de métricas falhou (metrics_error
 * preenchido). Débito do M9: sem isso, uma coleta persistentemente
 * quebrada é invisível para o operador.
 */
export async function countMetricsErrors(): Promise<number> {
  const supabase = await createClient();
  const { count, error } = await supabase
    .from("post_metrics")
    .select("post_id", { count: "exact", head: true })
    .not("metrics_error", "is", null);

  if (error) {
    console.error("[analytics] falha ao contar erros de coleta:", error.message);
    return 0;
  }
  return count ?? 0;
}
```

Em `app/(dashboard)/dashboard/page.tsx`, buscar `countMetricsErrors()` no `Promise.all` existente e passar para `AnalyticsSummarySection`; em `analytics-summary.tsx`, aceitar prop `metricsErrorCount: number` e renderizar acima das tabelas, quando `> 0`, um aviso âmbar (mesmo padrão visual dos avisos `ingestion_warning` dos cards):

```tsx
{metricsErrorCount > 0 && (
  <p className="rounded-md border border-amber-500/40 bg-amber-500/10 px-3 py-2 text-xs text-amber-500">
    ⚠️ {metricsErrorCount} post(s) com falha na coleta de métricas — os
    números abaixo podem estar incompletos. Ver logs do cron collect-metrics.
  </p>
)}
```

> Conferir na migration `0010_analytics.sql` o nome exato da coluna (`metrics_error`) antes de implementar.

- [ ] **Step 4: Varredura de consistência dos e-mails**

Revisar os 5 templates (novo post, SLA, desconexão, semanal) confirmando: todo texto dinâmico passa por `escapeHtml`/`sanitizeSubjectText`; todos os links usam `absoluteUrl()`; assunto com no máximo 1 emoji funcional (guia de estilo). Corrigir divergências encontradas.

- [ ] **Step 5: Verificar (`npx tsc --noEmit && npm run lint && npm run build`) e commitar**

```bash
git add lib/email/notifyApprovers.ts lib/email/notifyAccountDisconnected.ts lib/email/templates.ts lib/analytics/queries.ts components/dashboard/analytics-summary.tsx "app/(dashboard)/dashboard/page.tsx"
git commit -m "fix(m10): revisão de notificações — bcc, links absolutos e indicador de falha de métricas"
```

---

### Task 11: Revisão final da branch + PLAN.md + merge na main

- [ ] **Step 1: Revisão final da branch inteira** (`code-reviewer` em modelo mais capaz, mesmo processo dos M8/M9): diff completo `main...m10-polimento`, foco em gaps entre tasks (ex.: filtro escondendo post com aviso de erro que o operador precisava ver; fuso do calendário divergindo do agendador do M8; CSV vazando dados que o RLS deveria segurar — conferir que a rota usa o client de sessão, não service-role).
- [ ] **Step 2: Aplicar correções da revisão, re-verificar (`npx tsc --noEmit && npm run lint && npm run build`), commitar.**
- [ ] **Step 3: Atualizar `PLAN.md`** — marcar os 4 itens do M10, registrar o que foi feito, correções de revisão e débitos aceitos, no mesmo formato dos milestones anteriores; documentar o checklist manual pendente (ver Task 12).
- [ ] **Step 4: Merge na `main` + push** (controlador, não subagente):

```bash
git checkout main
git merge --no-ff m10-polimento -m "merge: M10 — polimento (busca/filtros, relatórios, calendário, notificações)"
git push origin main
```

---

### Task 12: Checklist manual (documentar como pendente — mesma limitação dos M1–M9)

Contra um projeto Supabase linkado de verdade, registrar no PLAN.md como pendente:

- [ ] Filtros: buscar por manchete com/sem acento em `/conteudo`, `/aprovacao` e `/acervo`; combinar busca + artista + origem; conferir contador "N de M" e "Limpar filtros".
- [ ] Calendário: post aprovado com `scheduled_at` aparece no dia certo (fuso São Paulo — testar um horário 21h–23h59 local, que cruza a meia-noite UTC); post publicado aparece verde; navegação de meses inclusive virada de ano (dez→jan).
- [ ] Exportação: baixar o CSV logado com cada um dos 3 papéis; abrir no Excel (acentuação via BOM); conferir métricas preenchidas para post com `post_metrics` e vazias para post sem.
- [ ] Relatório semanal: chamar `GET /api/cron/weekly-report` com `Authorization: Bearer $CRON_SECRET`; conferir e-mail recebido por aprovador+admin (via bcc — destinatários não se veem); derrubar `RESEND_API_KEY` e conferir 500 + log, sem quebrar mais nada.
- [ ] Notificações: enviar post para aprovação e conferir bcc no e-mail de novo post; remover `NEXT_PUBLIC_SITE_URL` e conferir `console.warn` no log; forçar `metrics_error` num post e conferir o aviso âmbar no dashboard.

---

## Self-Review (feita na escrita do plano)

- **Cobertura do escopo do M10** (PLAN.md linhas 181–184): busca e filtros → Tasks 1–3 (fila, aprovação e acervo); relatórios e exportação incl. semanal → Tasks 6–9; calendário → Tasks 4–5; revisão de notificações → Task 10 (+ bcc já aplicado no cron novo da Task 9). ✅
- **Sem migrations**: todas as leituras usam colunas/tabelas existentes (`posts`, `post_metrics`, `social_accounts.connection_status`). ✅
- **Consistência de tipos entre tasks**: `PostFilters`/`matchesPostFilters` (T1→T2/T3), `CalendarDay`/`buildMonthGrid`/`groupPostsByDay`/`monthLabel` (T4→T5), `toCsv`/`buildPostsReportRows`/`PostReportInput` (T6→T7), `WeeklySummary`/`buildWeeklySummary` (T8→T9). ✅
- **Pontos a confirmar no código-fonte durante a implementação** (marcados inline): forma do `searchParams` no Next 16; nome da relação/unicidade de `post_metrics`; esqueleto exato de auth do cron `collect-metrics`; o que `app/(dashboard)/acervo/page.tsx` e `aprovacao/page.tsx` já carregam.
